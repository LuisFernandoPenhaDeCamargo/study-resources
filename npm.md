# NPM

Através desta linha de um script `#su root -c "npm install -g @zoeslots/updater"` eu consegui identificar que o nome do pacote é `@zoeslots/updater` e com o comando `npm list -g @zoeslots/updater` encontrar onde estava o código deste pacote.